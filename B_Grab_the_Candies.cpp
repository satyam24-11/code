#include <bits/stdc++.h>
using namespace std;
#define ll long long
void solution()
{
  float n,c;
  cin>>n>>c;
  cout<<ceil(n/c)<<endl;
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin >> t;
    while (t--)
    {
        solution();
    }
    return 0;
}