#include <bits/stdc++.h>
using namespace std;
#define int long long
#define make_fast                          \
    std::ios_base::sync_with_stdio(false); \
    std::cin.tie(NULL);
const int mod = 1000000007;
void answer()
{
    int n;
    cin >> n;
    while(n--){
        int n1;
        cin >> n1;
        vector<int> v(n1);
        for (int i = 0; i < n1;i++){
            cin >> v[i];
        }
        int n2;
        cin >> n2;
        vector<int> v1(n2);
        for (int i = 0; i < n2;i++){
            cin >> v1[i];
        }
        
    }
}
int32_t main()
{
    make_fast;
    int t;
    cin >> t;
    while (t--)
    {
        answer();
    }
    return 0;
}